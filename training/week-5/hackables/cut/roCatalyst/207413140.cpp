#include <bits/stdc++.h>
using namespace std;
#define ll long long

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int t;
    cin >> t;
    while (t--) {
        ll n, c;
        cin >> n >> c;
        ll ans = 0;

        
        if (n==1) {
            float li;
            cin >> li;
            cout << ceil(li/(c+1)) << endl;
        }
        else {
            vector<ll> sticks;
        }
    }

    return 0;
}